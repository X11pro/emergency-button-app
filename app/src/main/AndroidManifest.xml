<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Emergency Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- Declaraciones de uso de permisos sensibles -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Declaración de uso de permisos sensibles para Google Play -->
    <!-- Location: Used to share exact GPS coordinates with emergency contacts during emergency activation -->
    <!-- SMS: Used to send emergency text messages to designated emergency contacts -->
    <!-- Phone: Used to make emergency calls to designated emergency contacts -->
    <!-- Contacts: Used to access emergency contact list for sending alerts -->

    <!-- Hardware Features -->
    <uses-feature android:name="android.hardware.location" android:required="true" />
    <uses-feature android:name="android.hardware.location.gps" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmergencyButton"
        tools:targetApi="35">

        <!-- Main Activity -->
        <activity
            android:name="com.emergency.sos.button.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.EmergencyButton.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Settings Activity -->
        <activity
            android:name="com.emergency.sos.button.SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.EmergencyButton.NoActionBar" />

        <!-- Widget Configuration Activity -->
        <activity
            android:name="com.emergency.sos.button.WidgetConfigurationActivity"
            android:exported="false"
            android:label="@string/sos_widget"
            android:parentActivityName=".SettingsActivity"
            android:theme="@style/Theme.EmergencyButton.NoActionBar" />

        <!-- Emergency Service -->
        <service
            android:name="com.emergency.sos.button.service.EmergencyService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location"
            android:permission="android.permission.FOREGROUND_SERVICE_LOCATION" />

        <!-- Floating SOS Service -->
        <service
            android:name="com.emergency.sos.button.service.FloatingSOSService"
            android:enabled="true"
            android:exported="false" />

        <!-- Emergency Widget -->
        <receiver
            android:name="com.emergency.sos.button.widget.EmergencyWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/emergency_widget_info" />
        </receiver>

        <!-- Boot Receiver for Widget -->
        <receiver
            android:name="com.emergency.sos.button.receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
